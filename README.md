# Tim Huang Signature Site — V5.1

V5.1 fixes the Expertise-card footer/tag collision by removing absolute positioning from `.tags` and using a flex-column card layout with `margin-top:auto`. This preserves aligned tag rows while allowing descriptions to wrap safely on desktop, tablet, and mobile.
